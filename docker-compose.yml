version: '3'

services:
  nginx:
    restart: always
    build:
      context: ./nginx
    ports:
      - '5100:80'
    depends_on:
      - webapp
  webapp:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - PORT=5000
